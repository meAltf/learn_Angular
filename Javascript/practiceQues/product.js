const product = {
    title: "Ball Pen parker",
    rating: 4,
    offer: 5,
    price: 350
};

console.log(product);
console.log(typeof(product));
console.log(product.title + " " + product.rating + " " + product.price);
